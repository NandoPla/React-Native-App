import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  buttonGood: {
    marginTop: 10,
    borderRadius: 5,
    padding: 5,
  },
  fontSizeText:{
    fontSize: 25,
    color: "white"
  },
})

export default styles
